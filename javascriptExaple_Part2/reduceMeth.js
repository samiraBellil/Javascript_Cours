// .reduce() = reduce the elments of an array in a single value
// The reduce() method does not change the original array.

/*const prices = [10, 50, 25, 30, 15];

const total = prices.reduce(sum);
console.log(total);

function sum(accumilateur, element) {
  return accumilateur + element;
}
*/
