//      function = a section of reusable code
//      function name(parameter1, parameter2, parameter3) {
//      code to be executed}
//      declare a code , used whetever you want
//      call the function to execute this code

/*function happyBirthday(username, age) {
  console.log('happy birthday to you!');
  console.log(`happy birthday dear ${username}`);
  console.log(`you are ${age}`);
}
happyBirthday('samira', 30);
*/

/*function add(x, y) {
  return x + y;
}
console.log(add(2, 3));*/

/*function substract(x, y) {
  return x - y;
}
function multiple(x, y) {
  return x * y;
}
console.log(substract(3, 2));
console.log(substract(3, 2));*/

/*function divide(x, y) {
  return x / y;
}
console.log(divide(6, 3));*/

//  variable scope javascript
//  local varible declare f wost function
//global variable declared outside the function
